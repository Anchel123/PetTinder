import Image from "next/image";

export default function Hero() {
    return (
        <div className="Hero">
            <section className="flex flex-col gap-8">
                <h1 className="w-full">Welcome to <span className="">{"PeTinder !"}</span></h1>
            </section>
        </div>
    )
}